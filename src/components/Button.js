import React from "react";
import "./Button.css";

const Button = () => {
    return (
        <div className="footer">
            <button type="button">Read More</button>
        </div>
    );
};

export default Button;
